{
  "version": 2,
  "builds": [
    {
      "src": "src/server.js",  // Adjust the path to server.js
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/src/server.js"  // Adjust the route to point to the correct location
    }
  ]
}
